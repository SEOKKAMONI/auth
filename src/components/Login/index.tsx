import * as S from "./style";

const Login = () => {
  return (
    <S.LoginLayout>
      {/* 로그인 id input, pw input, login button 이렇게 3개 있으면 됩니다 */}
      {/* 노션에 적은 규칙 보면서 작성 부탁드려요 */}
    </S.LoginLayout>
  );
};

export default Login;
